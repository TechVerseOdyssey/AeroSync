#!/usr/bin/env sh
# Same check as .github/workflows/rust.yml (fmt job).
set -e
cd "$(git rev-parse --show-toplevel)" || exit 1
if ! cargo fmt --all -- --check; then
	echo "rustfmt: workspace is not formatted. Run: cargo fmt --all" >&2
	exit 1
fi
