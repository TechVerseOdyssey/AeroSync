{
  "description": "Query the status of a background transfer task started by send_file or send_directory.",
  "input_schema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
      "mcp_auth_token": {
        "default": null,
        "description": "Local MCP auth token. Required only when the server has `AEROSYNC_MCP_SECRET` set.",
        "type": [
          "string",
          "null"
        ]
      },
      "task_id": {
        "description": "The task_id returned by `send_file` or `send_directory`.",
        "type": "string"
      }
    },
    "required": [
      "task_id"
    ],
    "title": "GetTransferStatusParams",
    "type": "object"
  }
}
